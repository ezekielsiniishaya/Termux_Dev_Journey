#include <stdio.h>

// Function to return the second largest number
int longest_substring(char str[]) { return 0; }

int main() {
  char s[13] = "Hello, world!";
  printf("String: %s\n", s);

  return 0;
}

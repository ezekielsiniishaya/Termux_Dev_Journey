// This program finds our the minimum number of resources needed for a meeting based on the time interval of the
// meetings

#include <stdio.h>

int main() {
  int intervals[3][2] = {{1, 10}, {2, 4}};
  printf("%d\n", intervals[0][1]);
  return 0;
}
